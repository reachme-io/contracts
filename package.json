{
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@tenderly/hardhat-tenderly": "^2.5.2",
    "hardhat": "^2.22.16"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.1.0",
    "dotenv": "^16.4.5"
  },
  "scripts": {
    "test": "npx hardhat test",
    "compile": "npx hardhat compile",
    "deploy": "npx hardhat ignition deploy ./ignition/modules/AbachiRedemption.js --network ethereum --deployment-id ethereum-deployment",
    "verify": "npx hardhat ignition verify ethereum-deployment",
    "deployverify": "npx hardhat ignition deploy ignition/modules/AbachiRedemption.js --network ethereum --verify"
  }
}
